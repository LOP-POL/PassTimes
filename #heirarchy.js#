const container = document.getElementById("container")
const elements = [1, 2, 3, 4, 5, 6]
const nameInput = document.getElementById("Name")
const childInput = document.getElementById("Child")
const form = document.getElementById("form")
const logs = document.getElementById("miniLogs")
// Select buttons using getElementsByClassName and add click event listeners
const addChildBtn = document.getElementsByClassName("addChild")[0];
const removeChildBtn = document.getElementsByClassName("removeChild")[0];
const parentLeftBtn = document.getElementsByClassName("parentLeft")[0];
const parentRightBtn = document.getElementsByClassName("parentRight")[0];
const levelUpBtn = document.getElementsByClassName("levelUp")[0];
const levelDownBtn = document.getElementsByClassName("levelDown")[0];

// interface Hier {
//     name:String,
//     children:Hier[]
// }

const followingChildren = []
const followingLevels = []
const levelCounter = 0
const currentLevel = 0
var rootNode = "root"
var currentParent = "root"
var currentChainId = 1
var nameGiven = ""
var childGiven = "" 

followingRoot.append(rootNode)

function miniLog(...log) {
    let finalLog = ""
    log.forEach((l) => {
        finalLog += "<br/> " + l + " "
    })
    logs.firstChild.innerHtml = finalLog

}

nameInput?.addEventListener("change", ((e) => {
    if (currentLevel == 0) {
        rootNode = e.target.value
        miniLog(`Changed root node to ${e.target.value}`)
    }
    nameGiven = e.target.value

}))
childInput?.addEventListener("change", ((e) => {
    miniLog(`newLog added ${e.target.value}`)
    childGiven = e.target.value
}))

form?.addEventListener("submit", ((e) => {
    e.preventDefault()
}))

var controlButtons = () => {
    let buttons = []
    if (form?.children.length) {
        for (var i = 0; i < form?.children.length; i++) {
            if (form?.children[i].tagName == "BUTTON") {
                buttons.push(form?.children[i])
            }
        }
    }

    return buttons
}

function addNewChildToDom() {

}

const chains = [{
    nodeName: "root",
    parentName: "root",
    level:1,
    children: [
        {
            nodeName: "root",
            parentName: "root",
            level:2,
            children: []
        },
        {
            nodeName: "root",
            parentName: "root",
            level:2,
            children: []
        }
    ]
}]

function traverseChain(level,itemAdded){
    let returnValue = itemAdded
    let depth;

    depth = chains[currentChainId]
   let  levelCounter = 
    while(depth.level != level){
        depth.children[0].
	levelCounter++
    }
    for(var i=0; i<level; i++){
        if(depth.level == level){
            level.
        }
    }
    return returnValue
}
addChildBtn?.addEventListener("click", (e) => {
    let newChild = 
});

removeChildBtn?.addEventListener("click", (e) => {

});

parentLeftBtn?.addEventListener("click", (e) => {

});

parentRightBtn?.addEventListener("click", (e) => {

});

levelUpBtn?.addEventListener("click", (e) => {

});

levelDownBtn?.addEventListener("click", (e) => {

});


